export const login = (data: LoginTypes): boolean => {
  // TODO: Add authentication
  console.log(data);

  return true;
};

export const logout = (): boolean => {
  // TODO: add Logout
  console.log("logged out");

  return true;
};
