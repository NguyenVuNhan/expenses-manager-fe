import React from "react";
import AuthenticationTemplate from "../components/templates/authentication.template";

const Login = () => {
  return <AuthenticationTemplate>Login</AuthenticationTemplate>;
};

export default Login;
